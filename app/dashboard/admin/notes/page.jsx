import { Typography } from "@mui/material";

const Notes = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Notes Page
      </Typography>
      <Typography>
        This is the notes page for admin. You can manage notes here.
      </Typography>
    </div>
  );
};

export default Notes;
